function solve([n]) {
     // text = "";
     // for (let i = 0; i < 5; i++) {
     // text += "*"; 
     // }
     // console.log(text);
        let star = "*";
       console.log(`${star.repeat(5)} - ${"Pesho".repeat(n)}`);

    }

    solve(["5"]);