function solve([n]) {
     // text = "";
     // for (let i = 0; i < 5; i++) {
     // text += "*"; 
     // }
     // console.log(text);
       console.log("*".repeat(5));
    }

    solve(["5"]);